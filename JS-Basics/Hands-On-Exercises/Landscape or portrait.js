// if the dimensions entered for width are greater thanthose of height
// the isLandscape function will return true it's a landscape otherwise portrait denoting false.

function isLandscape(width, height) {
  return width > height;
}

console.log(isLandscape(800, 600)); // calling isLandscape(); function
